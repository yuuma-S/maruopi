<% if end_user_signed_in? %>
	<% if question_answers.favorited_by?(current_end_user) %>
		<p><span>お気に入り解除: </span><%=link_to  question_answer_favorites_path(question_answers.id, answer), method: :delete do %>
			<i class="fa fa-heart" aria-hidden="true" style="color: red;"></i>
			<% end %>
			<%=question_answers.favorites.count %>
		</p>
	<% else %>
		<p><span>お気に入り登録: </span><%=link_to  question_answer_favorites_path(question_answers.id, answer), method: :post do %>
			<i class="fa fa-heart-o" aria-hidden="true"></i></i>
			<% end %>
			<%=question_answers.favorites.count %>
		</p>
	<% end %>
	<% else %>
	<p><span>お気に入り数: </span><%= question_answers.favorites.count %></p>
<% end %>